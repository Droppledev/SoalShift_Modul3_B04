#include <stdio.h>
#include <sys/ipc.h>
#include <sys/shm.h>
#include <unistd.h>
#include <string.h>


char weapon[6][10] ={"MP4A1", "PM2-V1", "SPR-3", "SS2-V5", "SPG1-V3","MINE"}
int quantity[6] 

void ListSenjata () {
    
}

int tambahStock (){
    char name[];
    int amount;
    int i;
    scanf ("%s %d", &nama, &amount);
    
    for (i=0; i<5; i++){
       if( strcmp (weapon[i], name));
            quantity[i] += amount;
    }
    
    
    
}
int main (){
    int menu;
    while (1){
        printf ("Pilih Menu 1 atau 2: ");
        printf ("1. Stock senjata\n2. tambah stock senjata");
        scanf ("%d", &menu);
        switch (menu){
            case 1:
                ListSenjata();
                break;
            case 2 :
                tambahStock();
                break;
        }
        
        
    }
}






